virtual_machines:
  - name: "${prefix}-vm1"
    location: "West Europe"
    size: "Standard_DS1_v2"
    tags:
      environment: "staging"
  - name: "${prefix}-vm2"
    location: "canadacentral"
    size: "Standard_DS2_v2"
    tags:
      environment: "production"
  # Add more virtual machines as needed
